/** Placeholder — diimplementasi pada micro-block auth berikutnya. */
export function LoginForm() {
  return (
    <p className="text-center text-sm text-muted-foreground">
      Form login akan terhubung ke Supabase Auth pada langkah berikutnya.
    </p>
  );
}
